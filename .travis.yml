sudo: required
services
- docker
install:
- pip install --user -r requirements.txt
# command to run tests
script:
# unit test
- python -m pytest -v
# linting test
- flake8

after_success:
- sh .travis/deploy_dockerhub.sh